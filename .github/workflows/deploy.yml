name: Deploy React Native Web

on:
  push:
    branches:
      - Appacella  # ✅ Change from "main" to "Appacella"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          ref: Appacella  # ✅ Ensure the correct branch is used

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 18
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install

      - name: Build for web
        run: yarn expo build:web

      - name: Move build files
        run: mv web-build public  # GitHub Pages expects "public" folder

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2  # ✅ Correct action for GitHub Pages
        with:
          path: public  # Upload the "public" folder

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v2  # ✅ Correct deploy action
